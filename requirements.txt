# 适配 Python 3.13 的核心依赖
streamlit>=1.36.0
numpy==1.26.4  # 唯一支持 Python 3.13 的稳定版
Pillow>=10.3.0

# PyTorch + TorchVision 适配 Python 3.13（最新 CPU 版）
torch==2.9.0+cpu
torchvision==0.24.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu
